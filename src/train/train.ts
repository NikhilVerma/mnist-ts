import { testDataPromise } from "../utils";

export function trainNeuralNetwork({
    testImages,
    testLabels,
    trainImages,
    trainLabels,
}: {
    testImages: Uint8ClampedArray[];
    testLabels: Uint8ClampedArray;
    trainImages: Uint8ClampedArray[];
    trainLabels: Uint8ClampedArray;
}) {}
